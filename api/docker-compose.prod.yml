services:
  deinscomplete-api:
    image: deinscomplete-api:${DEINSCOMPLETE_VERSION:-0.1.0-beta.1}
    env_file: .env
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
